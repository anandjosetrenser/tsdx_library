name: Node.js Package

on:
  push:
    branches:
      - master

jobs:
  publish-gpr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14
          registry-url: https://npm.pkg.github.com/
          scope: '@anandjosetrenser'
      - run: yarn install
      - run: yarn publish
        env:
          NODE_AUTH_TOKEN: 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
